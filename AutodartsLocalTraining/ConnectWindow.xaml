<Window x:Class="AutodartsLocalTraining.ConnectWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Autodarts Local Training - Connect"
        Width="380" Height="260"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Connect to Autodarts Board" FontSize="18" FontWeight="Bold"/>

        <StackPanel Grid.Row="2">
            <TextBlock Text="Board IP"/>
            <TextBox Text="{Binding Ip, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,0"/>
        </StackPanel>

        <StackPanel Grid.Row="4">
            <TextBlock Text="Port"/>
            <TextBox Text="{Binding Port, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,0"/>
        </StackPanel>

        <Button Grid.Row="6" Content="Connect" Height="32"
                Command="{Binding ConnectCommand}"/>

        <TextBlock Grid.Row="8" Text="{Binding StatusMessage}" Foreground="{Binding StatusBrush}" TextWrapping="Wrap"/>
    </Grid>
</Window>
