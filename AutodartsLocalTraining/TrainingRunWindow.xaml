<Window x:Class="AutodartsLocalTraining.TrainingRunWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Autodarts Local Training"
        Width="900" Height="600"
        WindowStartupLocation="CenterScreen"
        Background="Black">
    <DockPanel>
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="16">
            <TextBlock Text="{Binding BoardStatusText}" Foreground="{Binding BoardStatusBrush}"
                       VerticalAlignment="Center" FontSize="16" Margin="0,0,12,0"/>
            <Button Content="Disconnect" Padding="10,4" Command="{Binding DisconnectCommand}"/>
        </StackPanel>

        <Grid>
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="{Binding TrainingName}" Foreground="LightGray"
                           FontSize="24" HorizontalAlignment="Center" Margin="0,0,0,12"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock Text="{Binding TargetNumber}" Foreground="White"
                               FontSize="220" FontWeight="Bold"/>
                    <ItemsControl ItemsSource="{Binding Throws}" VerticalAlignment="Center" Margin="32,0,0,0">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Text}" Foreground="{Binding Foreground}"
                                           FontSize="48" FontWeight="Bold" Width="80"
                                           TextAlignment="Center" Margin="4,0"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
                <TextBlock Text="{Binding StatusText}" Foreground="LightGray"
                           FontSize="28" HorizontalAlignment="Center" Margin="0,0,0,24"/>
            </StackPanel>

            <TextBlock Text="{Binding Score}" Foreground="White"
                       FontSize="48" FontWeight="Bold"
                       HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,32,0"/>
        </Grid>
    </DockPanel>
</Window>
