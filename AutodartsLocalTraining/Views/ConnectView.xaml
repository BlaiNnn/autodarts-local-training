<UserControl x:Class="AutodartsLocalTraining.Views.ConnectView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Width="380" HorizontalAlignment="Center" VerticalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TitleText" Grid.Row="0" FontSize="18" FontWeight="Bold"/>

        <StackPanel Grid.Row="2">
            <TextBlock x:Name="BoardIpLabel"/>
            <TextBox Text="{Binding Ip, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,0"/>
        </StackPanel>

        <StackPanel Grid.Row="4">
            <TextBlock x:Name="PortLabel"/>
            <TextBox Text="{Binding Port, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,0"/>
        </StackPanel>

        <Button x:Name="ConnectButton" Grid.Row="6" Height="32"
                Command="{Binding ConnectCommand}"/>

        <TextBlock Grid.Row="8" Text="{Binding StatusMessage}" Foreground="{Binding StatusBrush}" TextWrapping="Wrap"/>
    </Grid>
</UserControl>
