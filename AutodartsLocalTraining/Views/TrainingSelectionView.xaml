<UserControl x:Class="AutodartsLocalTraining.WinUI.Views.TrainingSelectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DockPanel Margin="32">
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,0,0,16">
            <TextBlock Text="{Binding ConnectionText}" VerticalAlignment="Center"
                       FontSize="14" Margin="0,0,12,0"/>
            <Button x:Name="DisconnectButton" Command="{Binding DisconnectCommand}" Margin="0,0,8,0"/>
            <Button x:Name="QuitButton" Command="{Binding QuitCommand}"/>
        </StackPanel>

        <TextBlock x:Name="TitleText" DockPanel.Dock="Top" FontSize="22" FontWeight="Bold"
                    Margin="0,0,0,12"/>

        <ItemsControl ItemsSource="{Binding TrainingModes}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button Command="{Binding SelectCommand}" Width="240" Height="140"
                            Padding="20" Margin="0,0,16,16" HorizontalContentAlignment="Left"
                            VerticalContentAlignment="Top">
                        <StackPanel>
                            <TextBlock Text="{Binding Name}" FontSize="18" FontWeight="Bold"
                                       TextWrapping="Wrap" Margin="0,0,0,16"/>
                            <TextBlock Text="{Binding RecordText}" FontSize="14"/>
                            <TextBlock Text="{Binding RecordDateText}" FontSize="12" Opacity="0.7"/>
                        </StackPanel>
                    </Button>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </DockPanel>
</UserControl>
